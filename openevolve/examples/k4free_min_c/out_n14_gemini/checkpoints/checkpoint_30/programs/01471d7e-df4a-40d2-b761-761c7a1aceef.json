{"id": "01471d7e-df4a-40d2-b761-761c7a1aceef", "code": "\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n", "changes_description": null, "language": "python", "parent_id": "a06c1f89-1fb2-43d3-8bae-b54e18a5e858", "generation": 4, "timestamp": 1777018542.1072419, "iteration_found": 14, "metrics": {"valid": 1.0, "alpha": 4.0, "d_max": 6.0, "c_log": 0.9567610740878524, "combined_score": 1.0432389259121475, "reliability": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  else:\n          # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  else:\n          # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n          # This set {1, 3, 6} is K4-free for N >= 10 because:\n          # 1+3=4, 1+6=7, 3+6=9.\n          # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n          # This graph has degree 3 (undirected degree 6).\n          # It is a heuristic that this denser graph might have a smaller alpha.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3, 6}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END", "parent_metrics": {"valid": 1.0, "alpha": 4.0, "d_max": 4.0, "c_log": 0.8243971662222649, "combined_score": 1.1756028337777351, "reliability": 1.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert in extremal graph theory and combinatorial search.\nYou are evolving the Python function construct(N) to produce an\nN-vertex K4-free graph (no 4-clique) that minimises\n    c_log = alpha(G) * d_max(G) / (N * ln(d_max(G))).\nLower c_log is better. The benchmark is the Paley graph P(17) at\nc_log ~= 0.679; a random K4-free greedy sits around c_log ~= 1.1-1.2.\n\nHARD CONSTRAINTS (violating them gives score 0):\n  - construct(N) must return an N x N symmetric uint8 numpy adjacency\n    matrix with zero diagonal.\n  - The graph must be K4-free. A pragmatic check: K4-free iff every\n    neighbourhood N(v) is triangle-free.\n  - construct(N) must NOT raise on the given N. If a construction\n    only works for certain N (e.g. Paley needs N prime with N = 1\n    mod 4, circulant needs specific difference sets), fall back to\n    a different method when N does not fit.\n\nHelpers already imported at module scope (reuse them, do NOT\nreimplement):\n  is_k4_free(adj) -> bool\n  find_k4(adj)    -> (a,b,c,d) tuple or None\n  alpha_bb_clique_cover(adj) -> (alpha_value, mis_vertex_list)\n\nGood directions to explore:\n  - Structured seeds for general N: circulant C(N, S) with a\n    carefully chosen connection set S; bipartite / tripartite\n    blow-ups; Cayley graphs on small groups; products of small\n    K4-free graphs (Mycielski, Moebius-Kantor, Petersen).\n  - Near-regular degree sequences (pin d_max low).\n  - Local search / edge switching / tabu that preserves K4-freeness.\n  - Incremental K4 check on edge add (i,j): creates a K4 iff the\n    common neighbourhood N(i) & N(j) already induces an edge.\n\nThe fixed N for this run is set by the K4FREE_N environment variable\nand is read at the top of initial_program.py. Do not hardcode a\ndifferent N inside construct.\n", "user": "# Current Program Information\n- Fitness: 1.1756\n- Feature coordinates: \n- Focus areas: - Fitness improved: 0.5573 \u2192 1.1756\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### c_log\n```\n0.824397\n```\n\n### alpha\n```\n4\n```\n\n### d_max\n```\n4\n```\n\n### edges\n```\n28\n```\n\n### trials\n```\ntrial 0: c_log=0.8244 score=1.1756 (0.00s)\ntrial 1: c_log=0.8244 score=1.1756 (0.00s)\ntrial 2: c_log=0.8244 score=1.1756 (0.00s)\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  else:\n          # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  else:\n          # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\n- Metrics: valid: 1.0000, alpha: 7.0000, d_max: 4.0000, c_log: 1.4427, combined_score: 0.5573, reliability: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n  \n      Notes:\n        - N can be any integer >= 1. DO NOT raise on N; if a seed\n          (e.g. Paley) requires N \u2261 1 mod 4, fall back to another\n          construction, don't error out.\n        - Baseline: random-order greedy. Visit edges in a random\n          order; add an edge iff doing so keeps the graph K4-free.\n          Hovers around c_log \u2248 1.1\u20131.2 at N=14\u201315 \u2014 lots of room\n          to improve by picking smarter orderings, biasing degrees,\n          or seeding from structured families.\n      \"\"\"\n      rng = np.random.default_rng()\n      adj = np.zeros((N, N), dtype=np.uint8)\n      edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n      rng.shuffle(edges)\n  \n      # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n  ... (20 more lines)\nwith:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n      regular of degree 4 and K4-free. For N < 4, falls back to a\n      random-order K4-free greedy construction.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n      \"\"\"\n      if N < 4:\n          # For small N, random greedy is fine and avoids issues with circulant definitions.\n          rng = np.random.default_rng()\n          adj = np.zeros((N, N), dtype=np.uint8)\n          edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n          rng.shuffle(edges)\n  \n          # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n          # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n          nbr = [0] * N\n          for i, j in edges:\n              common = nbr[i] & nbr[j]\n  ... (26 more lines)\nChange 2: '# Original code to find and replace (must match exactly)' to '# New replacement code'\n- Metrics: valid: 1.0000, alpha: 4.0000, d_max: 4.0000, c_log: 0.8244, combined_score: 1.1756, reliability: 1.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nChange 2: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nChange 3: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\n- Metrics: valid: 1.0000, alpha: 4.0000, d_max: 4.0000, c_log: 0.8244, combined_score: 1.1756, reliability: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 1.1756)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (4.0000), Performs well on d_max (4.0000), Performs well on c_log (0.8244), Performs well on combined_score (1.1756), Performs well on reliability (1.0000)\n\n### Program 2 (Score: 1.1756)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (4.0000), Performs well on d_max (4.0000), Performs well on c_log (0.8244), Performs well on combined_score (1.1756), Performs well on reliability (1.0000)\n\n### Program 3 (Score: 0.5573)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (7.0000), Performs well on d_max (4.0000), Performs well on c_log (1.4427), Performs well on combined_score (0.5573), Performs well on reliability (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 0.5573, Type: Experimental)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 1.1756, Type: High-Performer)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program uses a circulant graph C(N, {1, 2}) for N >= 4. This graph has a fixed degree of 4. While it is K4-free, the goal is to minimize `c_log = alpha(G) * d_max(G) / (N * ln(d_max(G)))`. To potentially improve `c_log`, we should aim to reduce `alpha(G)` (the independence number) and `d_max(G)` (maximum degree), or at least find a better trade-off.\n\nThe current `alpha` is 4 for the circulant graph C(N, {1, 2}). The `d_max` is also 4. The `c_log` is 0.8244.\n\nThe Paley graph P(17) has `c_log` around 0.679. P(17) is a circulant graph C(17, {2, 13, 14, 15, 16}). This suggests that a different connection set for the circulant graph might yield better results.\n\nLet's try to explore circulant graphs with different connection sets. The connection set {1, 2} is quite compact. A more spread-out set might lead to a lower independence number.\n\nConsider the connection set {1, 3} for circulant graphs. For N=14, C(14, {1, 3}) has degree 4. Let's see how its alpha compares.\n\nThe previous attempt (Attempt 3) used C(N, {1, 3}) and resulted in a higher `c_log` (1.4427) and lower `combined_score` (0.5573). This suggests that simply changing the set to {1, 3} might not be sufficient or could even be detrimental.\n\nHowever, the problem statement mentions that circulant graphs are a good direction. The current implementation uses C(N, {1, 2}). Let's try a different set, like {1, 3}, but also consider that the \"best\" set might depend on N.\n\nAnother approach is to try and create a graph that is less regular, but still K4-free. The random greedy approach is used for N < 4. For N >= 4, it's always C(N, {1, 2}). We could try to combine these or introduce variations.\n\nLet's try to modify the circulant graph construction for N >= 4. Instead of a fixed set {1, 2}, we could try to generate a set of connections that aims to reduce alpha.\n\nFor a circulant graph C(N, S), it is K4-free if and only if for any x, y, z in S, x+y != z (mod N) and x != y.\n\nLet's consider a slightly more complex connection set that is still K4-free. For N=14, a set like {1, 2, 5} might be interesting. This would give a degree of 6.\n\nLet's analyze the current performance:\n- `c_log`: 0.8244\n- `alpha`: 4\n- `d_max`: 4\n\nWe want to reduce `c_log`. Since `d_max` is already low (4), reducing `alpha` would be the most direct way.\n\nLet's try to use a connection set that is known to produce smaller alpha values for circulant graphs. For example, for N=14, the set {1, 2, 3, 4} would create a K4 (e.g., 0-1-2-3 forms a K4 if all edges are present). So, the set must be chosen carefully.\n\nThe set {1, 2} for C(N, {1, 2}) is K4-free.\nThe set {1, 3} for C(N, {1, 3}) is also K4-free.\n\nLet's try a connection set that is more spread out, like {1, 4, 6} for N=14. This gives a degree of 6.\n\nThe issue is that the optimal connection set might be N-dependent.\n\nGiven the current program's success with C(N, {1, 2}) for N >= 4, and the fact that the `c_log` is relatively good (0.8244, better than the random greedy baseline), we should be cautious about drastic changes.\n\nHowever, the prompt encourages exploring different structured seeds.\n\nLet's consider a slightly different circulant graph construction. Instead of a fixed set {1, 2}, we can try to generate a set dynamically.\n\nFor N >= 4, let's try to use a connection set like `{1, 2, N//2 - 1, N//2}` if N is even, or `{1, 2, (N-1)//2, (N+1)//2}` if N is odd. This might create a graph with a slightly higher degree but potentially a lower alpha.\n\nLet's try a simpler modification first: change the connection set to `{1, 3}` for N >= 4, similar to what was tried in Attempt 3, but let's see if the evaluation results were misleading or if there's a reason to revisit. Attempt 3 had a worse c_log, so maybe {1, 3} is not good.\n\nWhat if we try to make the graph less regular? The current code has a fallback to random greedy for N < 4. For N >= 4, it always uses C(N, {1, 2}).\n\nLet's consider the possibility of using a bipartite graph construction for certain N. For example, if N is even, we can construct a complete bipartite graph K_{N/2, N/2}. This graph is triangle-free, and thus K4-free. The degree is N/2.\nFor N=14, K_{7,7} has degree 7. Alpha is 7.\nc_log = 7 * 7 / (14 * ln(7)) = 49 / (14 * 1.946) = 49 / 27.244 = 1.798. This is worse than the current 0.8244.\n\nSo, complete bipartite graphs are not a good direction for minimizing c_log in this case.\n\nLet's go back to circulant graphs. The key is to find a connection set S that is K4-free and results in a low alpha.\n\nFor N=14, C(14, {1, 2}) has alpha=4, d_max=4, c_log=0.8244.\nFor N=14, C(14, {1, 3}) has alpha=7, d_max=4, c_log=1.4427. This confirms Attempt 3's finding.\n\nWhat about a circulant graph with a different structure? The Paley graph P(q) is a circulant graph if q is prime and q = 1 mod 4. For N=17, P(17) is C(17, {2, 13, 14, 15, 16}). This has alpha=8, d_max=16.\nc_log = 8 * 16 / (17 * ln(16)) = 128 / (17 * 2.772) = 128 / 47.124 = 2.716.\nThis calculation seems off from the stated 0.679. Let me recheck the Paley graph properties.\n\nPaley graphs P(q) where q = 1 mod 4 are known to be strongly regular, and their independence number is (q-1)/2. The degree is (q-1)/2.\nFor P(17): q=17. Degree = (17-1)/2 = 8. Alpha = (17-1)/2 = 8.\nc_log = 8 * 8 / (17 * ln(8)) = 64 / (17 * 2.079) = 64 / 35.343 = 1.81.\n\nThe problem statement says Paley(17) is at c_log ~= 0.679. This implies that either my understanding of Paley graph properties or the formula for c_log is being applied differently, or the provided c_log value is for a different metric or a specific N.\n\nLet's assume the provided c_log benchmark is correct. The current program achieves 0.8244. We need to beat this.\n\nConsider the structure of C(N, S). For it to be K4-free, for any v, the neighborhood N(v) must be triangle-free. For C(N, S), N(v) = {v+s mod N | s in S}.\nIf S = {s1, s2}, then N(v) = {v+s1, v+s2}. This is always triangle-free because it only has 2 vertices.\nIf S = {s1, s2, s3}, then N(v) = {v+s1, v+s2, v+s3}. This neighborhood can contain a triangle. For example, if v+s1, v+s2, v+s3 form a triangle, then (v+s1) - (v+s2) - (v+s3) - (v+s1) must be edges. This means s1-s2, s2-s3, s3-s1 are in S (mod N).\nThe condition for C(N, S) to be K4-free is that no three elements s1, s2, s3 in S satisfy s1+s2 = s3 (mod N).\n\nFor S = {1, 2}, 1+2 = 3. If 3 is not in S, it's K4-free. C(N, {1, 2}) is K4-free.\nFor S = {1, 3}, 1+3 = 4. If 4 is not in S, it's K4-free. C(N, {1, 3}) is K4-free.\n\nLet's try to introduce a slightly larger, but still K4-free, connection set.\nFor N=14, consider S = {1, 2, 4}.\n1+2=3 (not in S)\n1+4=5 (not in S)\n2+4=6 (not in S)\nThis set is K4-free. The degree is 3.\nLet's calculate alpha for C(14, {1, 2, 4}). This is non-trivial to do by hand.\nIf alpha for C(14, {1, 2, 4}) is less than 4, this might be an improvement.\n\nThe current code uses a specific connection set {1, 2}. If N is small (N < 4), it falls back to random greedy. For N >= 4, it *always* uses C(N, {1, 2}). This is too rigid.\n\nA better approach would be to have a few different circulant graph constructions with different connection sets, and pick one that is suitable for N, or try to find one that has a good alpha.\n\nLet's try to implement a function that finds a \"good\" connection set for a given N. This is hard.\n\nInstead, let's try a fixed set of connection sets that are known to be K4-free and might offer variety.\nFor N >= 4, we can try:\n1. C(N, {1, 2}) - current, degree 2.\n2. C(N, {1, 3}) - degree 2.\n3. C(N, {1, 2, 4}) - degree 3. (K4-free if 3, 5, 6 are not in S).\n4. C(N, {1, 2, 5}) - degree 3. (K4-free if 3, 6, 7 are not in S).\n\nThe current program uses degree 2 (effectively degree 4 in the undirected graph). Let's try to introduce a circulant graph with degree 6 (connection set of size 3).\n\nFor N=14, let's try S = {1, 2, 5}.\nCheck K4-freeness:\n1+2=3 (not in S)\n1+5=6 (not in S)\n2+5=7 (not in S)\nThis set is K4-free. The degree is 3 (undirected degree 6).\n\nLet's propose changing the `else` block to use a slightly more complex connection set for N >= 4. This might increase `d_max` but could potentially decrease `alpha` more significantly, leading to a better `c_log`.\n\nThe current `c_log` is 0.8244.\n`alpha=4`, `d_max=4`, `N=14`.\n`c_log = 4 * 4 / (14 * ln(4)) = 16 / (14 * 1.386) = 16 / 19.404 = 0.8245`. This matches.\n\nIf we use C(N, {1, 2, 5}) with degree 6.\nSuppose alpha becomes 3.\n`c_log = 3 * 6 / (14 * ln(6)) = 18 / (14 * 1.791) = 18 / 25.074 = 0.717`. This would be a significant improvement.\n\nThe challenge is that alpha is not guaranteed to decrease.\n\nLet's try to introduce a conditional construction based on N.\nIf N is a prime of the form 4k+1, we could try to construct a Paley graph. However, the Paley graph construction is complex.\n\nA simpler approach: vary the connection set.\nLet's try to use a connection set that's a bit more spread out, like {1, 3, 6} for N=14.\n1+3=4 (not in S)\n1+6=7 (not in S)\n3+6=9 (not in S)\nThis is K4-free. Degree 3 (undirected degree 6).\n\nThe current code uses C(N, {1, 2}). Let's try C(N, {1, 3, 6}) as a replacement for N >= 4. This is a heuristic guess that a more spread-out set might reduce alpha.\n\n```python\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 3, 6}) which is\n    K4-free. For N < 4, falls back to a random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3, 6}) for N >= 4.\n        # This graph is K4-free if 1+3=4, 1+6=7, 3+6=9 are not in S (mod N).\n        # We need to ensure these conditions hold for the given N.\n        # For N=14: 4, 7, 9 are not in {1, 3, 6}.\n        # This set gives degree 3 (undirected degree 6).\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        \n        # Ensure the set {1, 3, 6} is K4-free for the given N\n        # We need to check if any combination of s1, s2, s3 in S sums to another element in S (mod N)\n        # or if s1+s2 = s3 (mod N)\n        # For S = {1, 3, 6}:\n        # 1+3 = 4. If 4 is in S or 4 = 1, 3, 6 mod N, it's a problem.\n        # 1+6 = 7. If 7 is in S or 7 = 1, 3, 6 mod N, it's a problem.\n        # 3+6 = 9. If 9 is in S or 9 = 1, 3, 6 mod N, it's a problem.\n\n        # The set {1, 3, 6} is K4-free if N is large enough such that 4, 7, 9 are not congruent to 1, 3, 6 mod N.\n        # For N=14, this is fine: 4, 7, 9 are not 1, 3, 6 mod 14.\n        # However, if N is small, e.g., N=7, then 1+6=7=0 mod 7, and 3+6=9=2 mod 7.\n        # If N=9, 3+6=9=0 mod 9.\n        # If N=10, 1+3=4, 1+6=7, 3+6=9. All are fine.\n        # If N=11, 1+3=4, 1+6=7, 3+6=9. All are fine.\n        # If N=12, 1+3=4, 1+6=7, 3+6=9. All are fine.\n        # If N=13, 1+3=4, 1+6=7, 3+6=9. All are fine.\n        # If N=14, 1+3=4, 1+6=7, 3+6=9. All are fine.\n\n        # This connection set {1, 3, 6} is generally K4-free for N >= 10.\n        # For smaller N, it might not be. We need a fallback.\n        # The prompt requires NOT to raise on given N.\n        # If N < 10, we should fall back to a safer construction.\n        # The random greedy is a safe fallback.\n\n        if N < 10: # Heuristic threshold for {1, 3, 6} to be safe.\n            # Fallback to random greedy for small N where {1, 3, 6} might not be K4-free.\n            rng = np.random.default_rng()\n            adj = np.zeros((N, N), dtype=np.uint8)\n            edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n            rng.shuffle(edges)\n\n            nbr = [0] * N\n            for i, j in edges:\n                common = nbr[i] & nbr[j]\n                creates_k4 = False\n                if common:\n                    tmp = common\n                    while tmp and not creates_k4:\n                        u = (tmp & -tmp).bit_length() - 1\n                        tmp &= tmp - 1\n                        if nbr[u] & common & ~(1 << u):\n                            creates_k4 = True\n                if not creates_k4:\n                    adj[i, j] = 1\n                    adj[j, i] = 1\n                    nbr[i] |= 1 << j\n                    nbr[j] |= 1 << i\n            return adj\n        else:\n            # Construct C(N, {1, 3, 6}) for N >= 10.\n            adj = np.zeros((N, N), dtype=np.uint8)\n            S = {1, 3, 6}\n            for i in range(N):\n                for s in S:\n                    neighbor = (i + s) % N\n                    adj[i, neighbor] = 1\n                    adj[neighbor, i] = 1\n            return adj\n# EVOLVE-BLOCK-END\n```\n\nThe reasoning for this change:\n1.  **Exploration of Structured Seeds**: The prompt encourages exploring different structured seeds. Circulant graphs are a good family.\n2.  **Improving `c_log`**: The current `c_log` is 0.8244. To improve it, we need to reduce `alpha` or `d_max` or find a better trade-off. The current graph has `alpha=4` and `d_max=4`.\n3.  **Connection Set {1, 3, 6}**: This set has size 3, leading to an undirected degree of 6. This increases `d_max` from 4 to 6. However, it is a heuristic guess that this more \"spread-out\" connection set might lead to a smaller independence number (`alpha`). If `alpha` decreases significantly (e.g., to 3), the product `alpha * d_max` might decrease, potentially lowering `c_log`.\n    *   For N=14, this set is K4-free.\n    *   The condition for K4-freeness in C(N, S) is that for any distinct s1, s2, s3 in S, s1 + s2 != s3 (mod N). For S = {1, 3, 6}, the sums are 1+3=4, 1+6=7, 3+6=9. If N is large enough (N >= 10), these sums will not be congruent to any element in S modulo N.\n4.  **Handling Small N**: For smaller values of N (N < 10), the set {1, 3, 6} might not be K4-free. To adhere to the \"must NOT raise\" constraint, a fallback to the random greedy construction (which is proven to be K4-free) is implemented for N < 10. This ensures robustness.\n5.  **Diversity**: This change introduces a different type of circulant graph (higher degree, potentially lower alpha) compared to the fixed degree-2 connection set.\n\nThis change aims to improve the `c_log` by exploring a different structured graph family (circulant graphs with a different connection set) while maintaining K4-freeness and robustness for all N.\n\nFinal check on the K4-freeness condition for C(N, {1, 3, 6}):\nWe need to ensure that for any distinct s_i, s_j, s_k in {1, 3, 6}, s_i + s_j != s_k (mod N).\nThe pairs of sums are:\n1 + 3 = 4\n1 + 6 = 7\n3 + 6 = 9\n\nSo, if N is such that 4, 7, or 9 are congruent to 1, 3, or 6 (mod N), then the graph might contain a K4.\n- If 4 = 1 (mod N) => N divides 3. N=3 (but we only use this for N>=4).\n- If 4 = 3 (mod N) => N divides 1. No solution for N>=4.\n- If 4 = 6 (mod N) => N divides 2. No solution for N>=4.\n- If 7 = 1 (mod N) => N divides 6. N=6.\n- If 7 = 3 (mod N) => N divides 4. N=4.\n- If 7 = 6 (mod N) => N divides 1. No solution for N>=4.\n- If 9 = 1 (mod N) => N divides 8. N=8.\n- If 9 = 3 (mod N) => N divides 6. N=6.\n- If 9 = 6 (mod N) => N divides 3. No solution for N>=4.\n\nSo, the connection set {1, 3, 6} is NOT K4-free for N=4, 6, 8.\nFor N=4: 7=3 mod 4.\nFor N=6: 7=1 mod 6, 9=3 mod 6.\nFor N=8: 9=1 mod 8.\n\nMy previous check was insufficient. The fallback condition needs to be more robust.\n\nLet's stick with the original {1, 2} for N < 10 and switch to a different set for N >= 10.\nThe set {1, 3, 6} is K4-free for N >= 10.\n\nThe fallback for N < 10 uses random greedy. This is fine.\nThe change is to use C(N, {1, 3, 6}) for N >= 10.\n\n```python\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 10, constructs a circulant graph C(N, {1, 3, 6}) which is\n    K4-free. For N < 10, falls back to a random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 10:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        # It's also guaranteed to be K4-free.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3, 6}) for N >= 10.\n        # This set is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n```\n\nThis revised change uses a threshold of N=10 for the {1, 3, 6} connection set, falling back to random greedy for N < 10. This is a safer approach to ensure K4-freeness while still exploring a different structured graph.\n\nLet's consider the `N` value for the current run. If `K4FREE_N` is set to 14, the new code will use C(14, {1, 3, 6}).\n\nThe previous `c_log` was 0.8244 with `alpha=4`, `d_max=4`.\nThe new graph C(N, {1, 3, 6}) has `d_max=6`.\nIf `alpha` remains 4, `c_log` would be `4 * 6 / (14 * ln(6)) = 24 / (14 * 1.791) = 24 / 25.074 = 0.957`. This is worse.\nIf `alpha` drops to 3, `c_log` would be `3 * 6 / (14 * ln(6)) = 18 / 25.074 = 0.717`. This is better.\n\nThe exact value of alpha for C(N, {1, 3, 6}) is not immediately obvious and depends on N. This is an exploratory step.\n\nThe current code is very specific for N >= 4. This change makes it slightly more adaptive by using a different construction for N >= 10.\n\nOne final check: the problem statement says \"The benchmark is the Paley graph P(17) at c_log ~= 0.679\". My calculations for Paley graph c_log were higher. If that benchmark is indeed achievable, then the current 0.8244 is far from it. This suggests that significant improvement is possible. The {1, 3, 6} set is a heuristic.\n\nIf this change makes `c_log` worse, the system's evaluation will reflect that. The goal is to suggest a change that *might* improve fitness.\n\nThe current program's `alpha` is 4, `d_max` is 4.\nThe proposed change uses a graph with `d_max = 6`.\nIf the `alpha` for this new graph is <= 3, then the `alpha * d_max` product might decrease:\nCurrent: 4 * 4 = 16\nProposed (if alpha=3): 3 * 6 = 18 (worse product)\nProposed (if alpha=2): 2 * 6 = 12 (better product)\n\nThe exact alpha is key.\n\nLet's consider another connection set for N >= 10: {1, 4, 7}.\n1+4=5, 1+7=8, 4+7=11.\nFor N=14: 5, 8, 11 are not in {1, 4, 7}. K4-free. Degree 6.\nSame logic applies.\n\nThe choice of {1, 3, 6} is arbitrary but represents a step towards a denser, potentially more complex structure.\n\nLet's stick with {1, 3, 6} for N >= 10.\n\n```python\n<<<<<<< SEARCH\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n=======\n    else:\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```\nThe search block should be from the `else:` statement onwards.\nThe `if N < 4:` block is not changed. The change is only for the `else:` part.\nThe original code in `else` uses `S={1, 2}`. The new code uses `S={1, 3, 6}`.\nThe original comment mentions `regular of degree 4`. The new comment mentions `degree 3 (undirected degree 6)`.\n\nLet me refine the change to"]}}, "artifacts_json": "{\"c_log\": \"0.956761\", \"alpha\": \"4\", \"d_max\": \"6\", \"edges\": \"42\", \"trials\": \"trial 0: c_log=0.9568 score=1.0432 (0.01s)\\ntrial 1: c_log=0.9568 score=1.0432 (0.00s)\\ntrial 2: c_log=0.9568 score=1.0432 (0.00s)\"}", "artifact_dir": null, "embedding": null}