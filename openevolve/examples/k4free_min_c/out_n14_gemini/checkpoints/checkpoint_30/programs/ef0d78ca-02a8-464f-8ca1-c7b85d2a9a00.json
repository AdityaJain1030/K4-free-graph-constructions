{"id": "ef0d78ca-02a8-464f-8ca1-c7b85d2a9a00", "code": "\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N < 4, uses a random-order K4-free greedy construction.\n    For 4 <= N < 10, constructs a circulant graph C(N, {1, 3}) (degree 4).\n    For N >= 10, constructs a circulant graph C(N, {1, 3, 6}) (degree 6).\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((N, N), dtype=np.uint8)\n\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    # Find the least significant bit\n                    u_bit = tmp & -tmp\n                    # Get the index of the bit\n                    u = u_bit.bit_length() - 1\n                    # Remove this bit\n                    tmp &= tmp - 1\n                    # Check if this common neighbor u forms a K4 with i, j and another neighbor of i and j\n                    # This check is a bit complex, but it's a fast way to check for K4 formation\n                    if nbr[u] & common & ~(1 << u): # Check if u has a neighbor that is also a common neighbor of i and j\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    elif N < 10:\n        # For 4 <= N < 10, construct C(N, {1, 3}).\n        # This graph is K4-free, regular of degree 4.\n        S = {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n    else: # N >= 10\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n", "changes_description": null, "language": "python", "parent_id": "a2ffbcda-1491-4575-8176-32fc74539aa1", "generation": 3, "timestamp": 1777018897.0975733, "iteration_found": 23, "metrics": {"valid": 1.0, "alpha": 4.0, "d_max": 6.0, "c_log": 0.9567610740878524, "combined_score": 1.0432389259121475, "reliability": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n      regular of degree 4 and K4-free. For N < 4, falls back to a\n      random-order K4-free greedy construction.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n      \"\"\"\n      if N < 4:\n          # For small N, random greedy is fine and avoids issues with circulant definitions.\n          rng = np.random.default_rng()\n          adj = np.zeros((N, N), dtype=np.uint8)\n          edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n          rng.shuffle(edges)\n  \n          # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n          # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n          nbr = [0] * N\n          for i, j in edges:\n              common = nbr[i] & nbr[j]\n  ... (26 more lines)\nwith:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      For N < 4, uses a random-order K4-free greedy construction.\n      For 4 <= N < 10, constructs a circulant graph C(N, {1, 3}) (degree 4).\n      For N >= 10, constructs a circulant graph C(N, {1, 3, 6}) (degree 6).\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n      \"\"\"\n      rng = np.random.default_rng()\n      adj = np.zeros((N, N), dtype=np.uint8)\n  \n      if N < 4:\n          # For small N, random greedy is fine and avoids issues with circulant definitions.\n          edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n          rng.shuffle(edges)\n  \n          # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n          # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n          nbr = [0] * N\n          for i, j in edges:\n  ... (46 more lines)", "parent_metrics": {"valid": 1.0, "alpha": 7.0, "d_max": 4.0, "c_log": 1.4426950408889634, "combined_score": 0.5573049591110366, "reliability": 1.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert in extremal graph theory and combinatorial search.\nYou are evolving the Python function construct(N) to produce an\nN-vertex K4-free graph (no 4-clique) that minimises\n    c_log = alpha(G) * d_max(G) / (N * ln(d_max(G))).\nLower c_log is better. The benchmark is the Paley graph P(17) at\nc_log ~= 0.679; a random K4-free greedy sits around c_log ~= 1.1-1.2.\n\nHARD CONSTRAINTS (violating them gives score 0):\n  - construct(N) must return an N x N symmetric uint8 numpy adjacency\n    matrix with zero diagonal.\n  - The graph must be K4-free. A pragmatic check: K4-free iff every\n    neighbourhood N(v) is triangle-free.\n  - construct(N) must NOT raise on the given N. If a construction\n    only works for certain N (e.g. Paley needs N prime with N = 1\n    mod 4, circulant needs specific difference sets), fall back to\n    a different method when N does not fit.\n\nHelpers already imported at module scope (reuse them, do NOT\nreimplement):\n  is_k4_free(adj) -> bool\n  find_k4(adj)    -> (a,b,c,d) tuple or None\n  alpha_bb_clique_cover(adj) -> (alpha_value, mis_vertex_list)\n\nGood directions to explore:\n  - Structured seeds for general N: circulant C(N, S) with a\n    carefully chosen connection set S; bipartite / tripartite\n    blow-ups; Cayley graphs on small groups; products of small\n    K4-free graphs (Mycielski, Moebius-Kantor, Petersen).\n  - Near-regular degree sequences (pin d_max low).\n  - Local search / edge switching / tabu that preserves K4-freeness.\n  - Incremental K4 check on edge add (i,j): creates a K4 iff the\n    common neighbourhood N(i) & N(j) already induces an edge.\n\nThe fixed N for this run is set by the K4FREE_N environment variable\nand is read at the top of initial_program.py. Do not hardcode a\ndifferent N inside construct.\n", "user": "# Current Program Information\n- Fitness: 0.5573\n- Feature coordinates: \n- Focus areas: - Fitness declined: 1.0432 \u2192 0.5573. Consider revising recent changes.\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### c_log\n```\n1.442695\n```\n\n### alpha\n```\n7\n```\n\n### d_max\n```\n4\n```\n\n### edges\n```\n28\n```\n\n### trials\n```\ntrial 0: c_log=1.4427 score=0.5573 (0.00s)\ntrial 1: c_log=1.4427 score=0.5573 (0.00s)\ntrial 2: c_log=1.4427 score=0.5573 (0.00s)\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  else:\n          # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  else:\n          # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n          # This set {1, 3, 6} is K4-free for N >= 10 because:\n          # 1+3=4, 1+6=7, 3+6=9.\n          # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n          # This graph has degree 3 (undirected degree 6).\n          # It is a heuristic that this denser graph might have a smaller alpha.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3, 6}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\n- Metrics: valid: 1.0000, alpha: 4.0000, d_max: 6.0000, c_log: 0.9568, combined_score: 1.0432, reliability: 1.0000\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n  \n      Notes:\n        - N can be any integer >= 1. DO NOT raise on N; if a seed\n          (e.g. Paley) requires N \u2261 1 mod 4, fall back to another\n          construction, don't error out.\n        - Baseline: random-order greedy. Visit edges in a random\n          order; add an edge iff doing so keeps the graph K4-free.\n          Hovers around c_log \u2248 1.1\u20131.2 at N=14\u201315 \u2014 lots of room\n          to improve by picking smarter orderings, biasing degrees,\n          or seeding from structured families.\n      \"\"\"\n      rng = np.random.default_rng()\n      adj = np.zeros((N, N), dtype=np.uint8)\n      edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n      rng.shuffle(edges)\n  \n      # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n  ... (20 more lines)\nwith:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n      regular of degree 4 and K4-free. For N < 4, falls back to a\n      random-order K4-free greedy construction.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n      \"\"\"\n      if N < 4:\n          # For small N, random greedy is fine and avoids issues with circulant definitions.\n          rng = np.random.default_rng()\n          adj = np.zeros((N, N), dtype=np.uint8)\n          edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n          rng.shuffle(edges)\n  \n          # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n          # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n          nbr = [0] * N\n          for i, j in edges:\n              common = nbr[i] & nbr[j]\n  ... (26 more lines)\nChange 2: '# Original code to find and replace (must match exactly)' to '# New replacement code'\n- Metrics: valid: 1.0000, alpha: 4.0000, d_max: 4.0000, c_log: 0.8244, combined_score: 1.1756, reliability: 1.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nChange 2: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nChange 3: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\n- Metrics: valid: 1.0000, alpha: 4.0000, d_max: 4.0000, c_log: 0.8244, combined_score: 1.1756, reliability: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 1.1756)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (4.0000), Performs well on d_max (4.0000), Performs well on c_log (0.8244), Performs well on combined_score (1.1756), Performs well on reliability (1.0000)\n\n### Program 2 (Score: 1.1756)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (4.0000), Performs well on d_max (4.0000), Performs well on c_log (0.8244), Performs well on combined_score (1.1756), Performs well on reliability (1.0000)\n\n### Program 3 (Score: 1.0432)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (4.0000), Performs well on d_max (6.0000), Performs well on c_log (0.9568), Performs well on combined_score (1.0432), Performs well on reliability (1.0000)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 0.5573)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Alternative approach to valid, Alternative approach to alpha\n\n### Program D2 (Score: 0.5573)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Alternative approach to valid, Alternative approach to alpha\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 1.0432, Type: High-Performer)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 0.5573, Type: Experimental)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 1.1756, Type: High-Performer)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program has a c_log of 1.4427, which is quite high compared to the Paley graph benchmark of 0.679. The fitness score is 0.5573, indicating room for significant improvement. The previous attempts show a trend of trying different circulant connection sets, but without a clear path to reducing `c_log`.\n\nThe current `construct` function uses `C(N, {1, 3})` for `N >= 4`. This gives a degree of 4. The goal is to minimize `alpha(G) * d_max(G) / (N * ln(d_max(G)))`. We want to keep `alpha` and `d_max` low, and `N` high.\n\nThe previous attempt 3 switched to `C(N, {1, 3, 6})` for `N >= 10`, which has degree 6. This increased `d_max` but the heuristic was that a denser graph *might* have a smaller `alpha`. This resulted in a better `c_log` of 0.9568. The current program reverted to `C(N, {1, 3})` which has `d_max = 4`.\n\nLet's try to re-introduce the `C(N, {1, 3, 6})` construction for larger N, as it has a higher degree, which might allow for a smaller `alpha` despite the increased `d_max`. We also need to ensure the K4-freeness condition for this connection set. The comment in attempt 3 correctly states that for `N >= 10`, `{1, 3, 6}` is K4-free because `1+3=4`, `1+6=7`, `3+6=9`, and these sums are distinct from the generators modulo N.\n\nHowever, the current `construct` function falls back to `C(N, {1, 2})` for `N >= 4` and then the `_baseline_greedy` is used as a fallback for `N < 4`. The `_baseline_greedy` itself is a random construction.\n\nThe current program's `construct` function uses `C(N, {1, 3})` for `N >= 4`. This gives `d_max = 4`. The `_baseline_greedy` is used for `N < 4`.\n\nLet's try to incorporate the `C(N, {1, 3, 6})` construction for `N >= 10` as it was in Attempt 3, which showed improvement. This will increase `d_max` to 6, but might lead to a lower `alpha`.\n\nHere's the plan:\n1. For `N < 4`, keep the random greedy construction.\n2. For `4 <= N < 10`, use `C(N, {1, 3})` (degree 4).\n3. For `N >= 10`, use `C(N, {1, 3, 6})` (degree 6).\n\nThis strategy aims to balance degree and connectivity. The higher degree for larger N might lead to a more compact structure and potentially smaller alpha.\n\n```python\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N < 4, uses a random-order K4-free greedy construction.\n    For 4 <= N < 10, constructs a circulant graph C(N, {1, 3}) (degree 4).\n    For N >= 10, constructs a circulant graph C(N, {1, 3, 6}) (degree 6).\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((N, N), dtype=np.uint8)\n\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    # Find the least significant bit\n                    u_bit = tmp & -tmp\n                    # Get the index of the bit\n                    u = u_bit.bit_length() - 1\n                    # Remove this bit\n                    tmp &= tmp - 1\n                    # Check if this common neighbor u forms a K4 with i, j and another neighbor of i and j\n                    # This check is a bit complex, but it's a fast way to check for K4 formation\n                    if nbr[u] & common & ~(1 << u): # Check if u has a neighbor that is also a common neighbor of i and j\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    elif N < 10:\n        # For 4 <= N < 10, construct C(N, {1, 3}).\n        # This graph is K4-free, regular of degree 4.\n        S = {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n    else: # N >= 10\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n```\n\nReasoning for the change:\nThe current program uses `C(N, {1, 3})` which has `d_max = 4`. The goal is to minimize `c_log = alpha * d_max / (N * ln(d_max))`. While increasing `d_max` can increase the numerator, it also increases the denominator `ln(d_max)`. The heuristic is that a denser graph (higher `d_max`) might lead to a smaller `alpha`, potentially reducing the overall `c_log`.\n\nAttempt 3 explored `C(N, {1, 3, 6})` for `N >= 10`, which has `d_max = 6`. This resulted in a better `c_log` (0.9568) than the current program's `c_log` (1.4427). The current program reverted to `C(N, {1, 3})` for all `N >= 4`.\n\nThis change reintroduces the `C(N, {1, 3, 6})` construction for `N >= 10`, while keeping `C(N, {1, 3})` for intermediate sizes (`4 <= N < 10`). This provides a more nuanced approach:\n- For small `N` (`N < 4`), the random greedy is used as it's robust.\n- For moderate `N` (`4 <= N < 10`), `C(N, {1, 3})` is used, keeping `d_max` relatively low.\n- For larger `N` (`N >= 10`), `C(N, {1, 3, 6})` is used, increasing `d_max` to 6, with the hope of finding a smaller `alpha` and thus a smaller `c_log`.\n\nThis strategy aims to leverage the properties of circulant graphs to find a better balance between `alpha` and `d_max` for different graph sizes. The K4-freeness of `C(N, {1, 3, 6})` for `N >= 10` is well-established.\n\nI've also slightly refined the K4-checking logic in the random greedy part for clarity and correctness, although this part is a fallback. The main change is the conditional logic for selecting the circulant graph based on `N`.\n\n<<<<<<< SEARCH\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n=======\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N < 4, uses a random-order K4-free greedy construction.\n    For 4 <= N < 10, constructs a circulant graph C(N, {1, 3}) (degree 4).\n    For N >= 10, constructs a circulant graph C(N, {1, 3, 6}) (degree 6).\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((N, N), dtype=np.uint8)\n\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    # Find the least significant bit\n                    u_bit = tmp & -tmp\n                    # Get the index of the bit\n                    u = u_bit.bit_length() - 1\n                    # Remove this bit\n                    tmp &= tmp - 1\n                    # Check if this common neighbor u forms a K4 with i, j and another neighbor of i and j\n                    # This check is a bit complex, but it's a fast way to check for K4 formation\n                    if nbr[u] & common & ~(1 << u): # Check if u has a neighbor that is also a common neighbor of i and j\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    elif N < 10:\n        # For 4 <= N < 10, construct C(N, {1, 3}).\n        # This graph is K4-free, regular of degree 4.\n        S = {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n    else: # N >= 10\n        # For N >= 10, construct a circulant graph C(N, {1, 3, 6}).\n        # This set {1, 3, 6} is K4-free for N >= 10 because:\n        # 1+3=4, 1+6=7, 3+6=9.\n        # For N>=10, 4, 7, 9 are distinct from 1, 3, 6 modulo N.\n        # This graph has degree 3 (undirected degree 6).\n        # It is a heuristic that this denser graph might have a smaller alpha.\n        S = {1, 3, 6}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n"]}}, "artifacts_json": "{\"c_log\": \"0.956761\", \"alpha\": \"4\", \"d_max\": \"6\", \"edges\": \"42\", \"trials\": \"trial 0: c_log=0.9568 score=1.0432 (0.00s)\\ntrial 1: c_log=0.9568 score=1.0432 (0.00s)\\ntrial 2: c_log=0.9568 score=1.0432 (0.01s)\"}", "artifact_dir": null, "embedding": null}