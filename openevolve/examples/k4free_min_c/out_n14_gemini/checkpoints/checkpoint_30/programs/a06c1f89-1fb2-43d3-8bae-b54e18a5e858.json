{"id": "a06c1f89-1fb2-43d3-8bae-b54e18a5e858", "code": "\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n", "changes_description": null, "language": "python", "parent_id": "a2ffbcda-1491-4575-8176-32fc74539aa1", "generation": 3, "timestamp": 1777018481.669247, "iteration_found": 11, "metrics": {"valid": 1.0, "alpha": 4.0, "d_max": 4.0, "c_log": 0.8243971662222649, "combined_score": 1.1756028337777351, "reliability": 1.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nChange 2: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nChange 3: Replace:\n  # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END", "parent_metrics": {"valid": 1.0, "alpha": 7.0, "d_max": 4.0, "c_log": 1.4426950408889634, "combined_score": 0.5573049591110366, "reliability": 1.0}, "island": 1}, "prompts": {"diff_user": {"system": "You are an expert in extremal graph theory and combinatorial search.\nYou are evolving the Python function construct(N) to produce an\nN-vertex K4-free graph (no 4-clique) that minimises\n    c_log = alpha(G) * d_max(G) / (N * ln(d_max(G))).\nLower c_log is better. The benchmark is the Paley graph P(17) at\nc_log ~= 0.679; a random K4-free greedy sits around c_log ~= 1.1-1.2.\n\nHARD CONSTRAINTS (violating them gives score 0):\n  - construct(N) must return an N x N symmetric uint8 numpy adjacency\n    matrix with zero diagonal.\n  - The graph must be K4-free. A pragmatic check: K4-free iff every\n    neighbourhood N(v) is triangle-free.\n  - construct(N) must NOT raise on the given N. If a construction\n    only works for certain N (e.g. Paley needs N prime with N = 1\n    mod 4, circulant needs specific difference sets), fall back to\n    a different method when N does not fit.\n\nHelpers already imported at module scope (reuse them, do NOT\nreimplement):\n  is_k4_free(adj) -> bool\n  find_k4(adj)    -> (a,b,c,d) tuple or None\n  alpha_bb_clique_cover(adj) -> (alpha_value, mis_vertex_list)\n\nGood directions to explore:\n  - Structured seeds for general N: circulant C(N, S) with a\n    carefully chosen connection set S; bipartite / tripartite\n    blow-ups; Cayley graphs on small groups; products of small\n    K4-free graphs (Mycielski, Moebius-Kantor, Petersen).\n  - Near-regular degree sequences (pin d_max low).\n  - Local search / edge switching / tabu that preserves K4-freeness.\n  - Incremental K4 check on edge add (i,j): creates a K4 iff the\n    common neighbourhood N(i) & N(j) already induces an edge.\n\nThe fixed N for this run is set by the K4FREE_N environment variable\nand is read at the top of initial_program.py. Do not hardcode a\ndifferent N inside construct.\n", "user": "# Current Program Information\n- Fitness: 0.5573\n- Feature coordinates: \n- Focus areas: - Fitness unchanged at 0.5573\n- No feature coordinates\n- Consider simplifying - code length exceeds 500 characters\n\n## Last Execution Output\n\n### c_log\n```\n1.442695\n```\n\n### alpha\n```\n7\n```\n\n### d_max\n```\n4\n```\n\n### edges\n```\n28\n```\n\n### trials\n```\ntrial 0: c_log=1.4427 score=0.5573 (0.00s)\ntrial 1: c_log=1.4427 score=0.5573 (0.00s)\ntrial 2: c_log=1.4427 score=0.5573 (0.00s)\n```\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  else:\n          # Construct C(N, {1, 2}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 2}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\nwith:\n  else:\n          # Construct C(N, {1, 3}) for N >= 4.\n          # This graph is K4-free, regular of degree 4.\n          adj = np.zeros((N, N), dtype=np.uint8)\n          S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n          for i in range(N):\n              for s in S:\n                  neighbor = (i + s) % N\n                  adj[i, neighbor] = 1\n                  adj[neighbor, i] = 1\n          return adj\n  # EVOLVE-BLOCK-END\n- Metrics: valid: 1.0000, alpha: 7.0000, d_max: 4.0000, c_log: 1.4427, combined_score: 0.5573, reliability: 1.0000\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n  \n      Notes:\n        - N can be any integer >= 1. DO NOT raise on N; if a seed\n          (e.g. Paley) requires N \u2261 1 mod 4, fall back to another\n          construction, don't error out.\n        - Baseline: random-order greedy. Visit edges in a random\n          order; add an edge iff doing so keeps the graph K4-free.\n          Hovers around c_log \u2248 1.1\u20131.2 at N=14\u201315 \u2014 lots of room\n          to improve by picking smarter orderings, biasing degrees,\n          or seeding from structured families.\n      \"\"\"\n      rng = np.random.default_rng()\n      adj = np.zeros((N, N), dtype=np.uint8)\n      edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n      rng.shuffle(edges)\n  \n      # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n  ... (20 more lines)\nwith:\n  # EVOLVE-BLOCK-START\n  def construct(N: int) -> np.ndarray:\n      \"\"\"\n      Build an N-vertex K4-free graph aimed at small c_log.\n  \n      For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n      regular of degree 4 and K4-free. For N < 4, falls back to a\n      random-order K4-free greedy construction.\n  \n      Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n      diagonal. Must be K4-free; the evaluator rejects non-K4-free\n      outputs (combined_score = 0).\n  \n      Helpers available at module scope (already imported, DO NOT\n      redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n      `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n      these to hand-rolled K4/\u03b1 checks.\n      \"\"\"\n      if N < 4:\n          # For small N, random greedy is fine and avoids issues with circulant definitions.\n          rng = np.random.default_rng()\n          adj = np.zeros((N, N), dtype=np.uint8)\n          edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n          rng.shuffle(edges)\n  \n          # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n          # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n          nbr = [0] * N\n          for i, j in edges:\n              common = nbr[i] & nbr[j]\n  ... (26 more lines)\nChange 2: '# Original code to find and replace (must match exactly)' to '# New replacement code'\n- Metrics: valid: 1.0000, alpha: 4.0000, d_max: 4.0000, c_log: 0.8244, combined_score: 1.1756, reliability: 1.0000\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 1.1756)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (4.0000), Performs well on d_max (4.0000), Performs well on c_log (0.8244), Performs well on combined_score (1.1756), Performs well on reliability (1.0000)\n\n### Program 2 (Score: 0.5573)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nKey features: Performs well on valid (1.0000), Performs well on alpha (7.0000), Performs well on d_max (4.0000), Performs well on c_log (1.4427), Performs well on combined_score (0.5573), Performs well on reliability (1.0000)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 1.1756, Type: High-Performer)\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"\nK4-free min-c_log search \u2014 OpenEvolve target program.\n\nThe LLM evolves `construct(N)` to return a K4-free graph that minimises\n    c_log(G) = alpha(G) * d_max(G) / (N * ln(d_max(G))).\n\nPaley(17) sits at c_log \u2248 0.679; heuristic baselines plateau near 0.94.\nThis initial_program starts where the AlphaEvolve Ramsey setup does: a\nrandom seed with a cheap K4-free greedy, no algebraic priors.\n\"\"\"\n\nimport os\nimport sys\n\nimport numpy as np\n\n# Expose the vendored helpers at module scope so evolved versions of\n# construct() can call them without re-implementing K4 detection.\n_HERE = os.path.dirname(os.path.abspath(__file__))\nif _HERE not in sys.path:\n    sys.path.insert(0, _HERE)\nfrom graph_utils import is_k4_free, find_k4, alpha_bb_clique_cover  # noqa: E402\n\n\n# Fixed problem size. Set via env var so one file serves N=14 and N=15.\nN = int(os.environ.get(\"K4FREE_N\", \"14\"))\n\n\n# EVOLVE-BLOCK-START\ndef construct(N: int) -> np.ndarray:\n    \"\"\"\n    Build an N-vertex K4-free graph aimed at small c_log.\n\n    For N >= 4, constructs a circulant graph C(N, {1, 2}) which is\n    regular of degree 4 and K4-free. For N < 4, falls back to a\n    random-order K4-free greedy construction.\n\n    Returns an N x N symmetric 0/1 uint8 adjacency matrix with zero\n    diagonal. Must be K4-free; the evaluator rejects non-K4-free\n    outputs (combined_score = 0).\n\n    Helpers available at module scope (already imported, DO NOT\n    redefine): `is_k4_free(adj) -> bool`, `find_k4(adj) -> tuple|None`,\n    `alpha_bb_clique_cover(adj) -> (alpha, mis_vertices)`. Prefer\n    these to hand-rolled K4/\u03b1 checks.\n    \"\"\"\n    if N < 4:\n        # For small N, random greedy is fine and avoids issues with circulant definitions.\n        rng = np.random.default_rng()\n        adj = np.zeros((N, N), dtype=np.uint8)\n        edges = [(i, j) for i in range(N) for j in range(i + 1, N)]\n        rng.shuffle(edges)\n\n        # Track neighbour bitmasks for O(1) K4 check on an added edge (i, j):\n        # adding (i, j) creates a K4 iff N(i) \u2229 N(j) already contains an edge.\n        nbr = [0] * N\n        for i, j in edges:\n            common = nbr[i] & nbr[j]\n            creates_k4 = False\n            if common:\n                tmp = common\n                while tmp and not creates_k4:\n                    u = (tmp & -tmp).bit_length() - 1\n                    tmp &= tmp - 1\n                    if nbr[u] & common & ~(1 << u):\n                        creates_k4 = True\n            if not creates_k4:\n                adj[i, j] = 1\n                adj[j, i] = 1\n                nbr[i] |= 1 << j\n                nbr[j] |= 1 << i\n        return adj\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n\n\ndef _baseline_greedy(n: int) -> np.ndarray:\n    \"\"\"Random-order K4-free greedy \u2014 guaranteed-valid fallback.\"\"\"\n    rng = np.random.default_rng()\n    adj = np.zeros((n, n), dtype=np.uint8)\n    edges = [(i, j) for i in range(n) for j in range(i + 1, n)]\n    rng.shuffle(edges)\n    nbr = [0] * n\n    for i, j in edges:\n        common = nbr[i] & nbr[j]\n        creates_k4 = False\n        if common:\n            tmp = common\n            while tmp and not creates_k4:\n                u = (tmp & -tmp).bit_length() - 1\n                tmp &= tmp - 1\n                if nbr[u] & common & ~(1 << u):\n                    creates_k4 = True\n        if not creates_k4:\n            adj[i, j] = 1\n            adj[j, i] = 1\n            nbr[i] |= 1 << j\n            nbr[j] |= 1 << i\n    return adj\n\n\ndef run_search() -> np.ndarray:\n    \"\"\"\n    Entry point called by the evaluator. Always returns a valid\n    K4-free adjacency matrix: if the evolved construct(N) raises,\n    returns the wrong shape/type, or produces a non-K4-free graph,\n    we fall back to the random-greedy baseline so the evaluator\n    still gets a scorable graph (\u2248 c_log 1.10\u20131.17 at N=14\u201315).\n    \"\"\"\n    try:\n        adj = construct(N)\n        if not isinstance(adj, np.ndarray):\n            adj = np.asarray(adj)\n        if adj.ndim != 2 or adj.shape != (N, N):\n            raise ValueError(f\"construct returned shape {adj.shape}, expected ({N},{N})\")\n        adj = ((adj + adj.T) > 0).astype(np.uint8)\n        np.fill_diagonal(adj, 0)\n        if not is_k4_free(adj):\n            raise ValueError(\"construct returned a graph containing a K4\")\n        return adj\n    except Exception:\n        return _baseline_greedy(N)\n\n\nif __name__ == \"__main__\":\n    adj = run_search()\n    print(f\"N={N} edges={int(adj.sum() // 2)} d_max={int(adj.sum(axis=1).max())}\")\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: complexity, diversity\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program uses a circulant graph $C(N, \\{1, 3\\})$ for $N \\geq 4$. This graph has a regular degree of 4. While this is K4-free, it might not be optimal for minimizing $c_{log} = \\alpha(G) \\cdot d_{max}(G) / (N \\cdot \\ln(d_{max}(G)))$. The fact that $d_{max}$ is fixed at 4 might be a limiting factor.\n\nThe Paley graph $P(17)$ has $c_{log} \\approx 0.679$. This suggests we should aim for graphs with lower $\\alpha(G)$ and potentially higher degrees if they lead to a much lower $\\alpha(G)$.\n\nLet's consider alternative structured graphs that are K4-free.\n\n1.  **Mycielski graphs:** The $k$-th Mycielski graph $M_k$ is triangle-free (and thus K4-free). $M_3$ is the Gr\u00f6tzsch graph (11 vertices, 20 edges, $\\alpha=5$, $d_{max}=5$). $M_4$ has 23 vertices, 70 edges, $\\alpha=9$, $d_{max}=10$. These graphs have a structure that tends to keep the independence number low relative to the number of vertices.\n\n2.  **Moore graphs:** While not directly K4-free in general, some specific constructions related to Moore graphs or their generalizations could be explored.\n\n3.  **Bipartite graphs:** Bipartite graphs are K3-free, and thus K4-free. If we can construct a \"dense\" bipartite graph, it might be a good candidate. However, $\\alpha(G)$ for a bipartite graph is $N/2$.\n\nGiven the constraint of not raising errors for any $N$, and the need for a K4-free graph, a fallback to the random greedy is a good safety net.\n\nThe current program's $c_{log}$ is 1.4427, which is significantly higher than the Paley graph benchmark. The main lever we have is changing the construction for $N \\geq 4$.\n\nLet's try to construct a graph that might have a better balance of $\\alpha(G)$ and $d_{max}$.\n\nOne approach is to consider a \"blow-up\" of a smaller K4-free graph. For instance, if we have a K4-free graph $H$ on $k$ vertices, we can create a graph on $N$ vertices by replacing each vertex of $H$ with a clique of size $N/k$ (if $N$ is divisible by $k$). However, this is complex to implement generally and might not guarantee K4-freeness if the original graph is not \"sufficiently\" K4-free.\n\nA simpler approach for structured graphs is to use known constructions that are K4-free. The circulant graph $C(N, S)$ is a good family. The choice of $S$ is crucial. For $S=\\{1, 2\\}$, $C(N, \\{1, 2\\})$ is 4-regular and K4-free. For $S=\\{1, 3\\}$, it's also 4-regular and K4-free.\n\nLet's consider if we can achieve a lower $c_{log}$ by increasing the degree, hoping for a significant drop in $\\alpha(G)$.\n\nConsider $C(N, \\{1, 2, 3\\})$ for $N \\geq 4$. This graph has degree 6.\nIf $N$ is large enough, $C(N, \\{1, 2, 3\\})$ is K4-free.\nLet's check this property: A circulant graph $C(N, S)$ is $K_4$-free if and only if there are no $a, b, c, d \\in \\{0, \\dots, N-1\\}$ such that $a-b, b-c, c-d, d-a \\in S$ modulo $N$, and $a-c, b-d \\in S$ modulo $N$. A simpler condition for $K_4$-freeness is that for any $v$, the neighborhood $N(v)$ is triangle-free. For $C(N, \\{1, 2, 3\\})$, $N(v) = \\{v+1, v-1, v+2, v-2, v+3, v-3\\} \\pmod N$. For $N$ large enough, this neighborhood is triangle-free. For example, if $N=14$, $S=\\{1, 2, 3\\}$, degree is 6.\n$N(0) = \\{1, 13, 2, 12, 3, 11\\}$.\nTriangles in $N(0)$:\n- (1, 2, 3)? $1-2 \\in \\{-1\\}$, $1-3 \\in \\{-2\\}$, $2-3 \\in \\{-1\\}$. $S$ contains $1, 2, 3$. We need to check if $1, 2, 3$ form a triangle. $1$ is connected to $2$ and $3$. Is $2$ connected to $3$? Yes, $2-3 = -1 \\equiv 13 \\pmod{14}$, and $1 \\in S$. So $(1, 2, 3)$ is a triangle. Thus $C(14, \\{1, 2, 3\\})$ is NOT K4-free.\n\nThe condition for $C(N, S)$ to be K4-free is subtle. A simpler known K4-free construction is the Petersen graph (10 vertices, 15 edges, $\\alpha=4$, $d_{max}=3$) or the Moebius-Kantor graph (16 vertices, 24 edges, $\\alpha=8$, $d_{max}=3$).\n\nThe current program uses $C(N, \\{1, 3\\})$. This means $N(v) = \\{v+1, v-1, v+3, v-3\\} \\pmod N$.\nFor $N=14$, $N(0) = \\{1, 13, 3, 11\\}$.\nAre there any edges between these nodes?\n- $1$ and $3$? $1-3 = -2 \\equiv 12 \\pmod{14}$. Is $2$ or $-2$ in $S$? No.\n- $1$ and $11$? $1-11 = -10 \\equiv 4 \\pmod{14}$. Is $4$ or $-4$ in $S$? No.\n- $1$ and $13$? $1-13 = -12 \\equiv 2 \\pmod{14}$. Is $2$ or $-2$ in $S$? No.\n- $3$ and $11$? $3-11 = -8 \\equiv 6 \\pmod{14}$. Is $6$ or $-6$ in $S$? No.\n- $3$ and $13$? $3-13 = -10 \\equiv 4 \\pmod{14}$. Is $4$ or $-4$ in $S$? No.\n- $11$ and $13$? $11-13 = -2 \\equiv 12 \\pmod{14}$. Is $2$ or $-2$ in $S$? No.\n\nSo, $N(v)$ is triangle-free for $C(14, \\{1, 3\\})$. This confirms it's K4-free.\n\nThe problem is that $d_{max}$ is fixed at 4, and $\\alpha$ is likely not as low as it could be.\n\nLet's try a specific construction that is known to be K4-free and might offer better properties. The graph formed by taking two disjoint copies of $K_m$ and adding edges between corresponding vertices is a $K_{m+1}$-free graph. This isn't quite what we need.\n\nConsider the structure of the problem: we want to minimize $\\alpha(G) \\cdot d_{max}(G)$.\nIf we can find a graph with a low $\\alpha(G)$ even at a slightly higher $d_{max}$, it might be better.\n\nLet's consider $N=14$.\nCurrent: $C(14, \\{1, 3\\})$. $d_{max}=4$.\n$\\alpha$ for $C(14, \\{1, 3\\})$: Need to compute this. If $\\alpha$ is, say, 6, then $\\alpha \\cdot d_{max} = 24$.\n$c_{log} = 24 / (14 \\cdot \\ln(4)) \\approx 24 / (14 \\cdot 1.386) \\approx 24 / 19.404 \\approx 1.236$. This is lower than 1.4427.\n\nLet's try to find a construction that yields a lower $\\alpha$ or a better balance.\n\nA common strategy for K4-free graphs is to use a construction that is $K_3$-free on neighborhoods.\n\nWhat if we try to construct a graph with a slightly higher degree but a significantly lower $\\alpha$?\nFor $N \\ge 6$, consider $C(N, \\{1, 2\\})$. This has degree 4.\n$N(0) = \\{1, N-1, 2, N-2\\}$.\nFor $N=14$, $S=\\{1, 2\\}$. $N(0)=\\{1, 13, 2, 12\\}$.\nTriangles in $N(0)$:\n- $(1, 2, ?)$: $1$ is connected to $2$. Is $1$ connected to $13$? Yes. Is $2$ connected to $13$? $2-13 = -11 \\equiv 3 \\pmod{14}$. $3 \\not\\in S$.\n- $(1, 2, ?)$: $1$ is connected to $2$. Is $1$ connected to $12$? $1-12 = -11 \\equiv 3 \\pmod{14}$. $3 \\not\\in S$.\n- $(1, 13, ?)$: $1$ is connected to $13$. Is $1$ connected to $2$? Yes. Is $13$ connected to $2$? $13-2 = 11 \\equiv -3 \\pmod{14}$. $-3 \\not\\in S$.\n- $(1, 13, ?)$: $1$ is connected to $13$. Is $1$ connected to $12$? $1-12 = -11 \\equiv 3 \\pmod{14}$. $3 \\not\\in S$.\n- $(2, 12, ?)$: $2$ is connected to $12$. Is $2$ connected to $1$? Yes. Is $12$ connected to $1$? $12-1 = 11 \\equiv -3 \\pmod{14}$. $-3 \\not\\in S$.\n- $(2, 12, ?)$: $2$ is connected to $12$. Is $2$ connected to $13$? $2-13 = -11 \\equiv 3 \\pmod{14}$. $3 \\not\\in S$.\nSo $C(14, \\{1, 2\\})$ is also K4-free.\n\nThe change from $C(N, \\{1, 2\\})$ to $C(N, \\{1, 3\\})$ in the history did not significantly improve the score (it went from 1.1756 to 0.5573, but the score metric is inverted, so it got worse). The $c_{log}$ for $C(N, \\{1, 2\\})$ was 0.8244. The $c_{log}$ for $C(N, \\{1, 3\\})$ is 1.4427. This is counter-intuitive; usually $S=\\{1, 2\\}$ is considered \"more connected\" and might lead to smaller $\\alpha$.\n\nLet's re-examine the initial program's structure. It falls back to random greedy for $N < 4$. This is good. For $N \\geq 4$, it uses $C(N, \\{1, 3\\})$.\n\nThe problem statement mentions: \"The benchmark is the Paley graph P(17) at c_log ~= 0.679\". This is a very strong benchmark. Our current $c_{log}$ is 1.4427. We need to significantly reduce $\\alpha \\cdot d_{max}$.\n\nConsider $N=14$ for the fixed size.\nCurrent: $C(14, \\{1, 3\\})$, $d_{max}=4$, $c_{log}=1.4427$.\nPrevious best: $C(14, \\{1, 2\\})$, $d_{max}=4$, $c_{log}=0.8244$.\n\nThis suggests that $C(N, \\{1, 2\\})$ is a better choice than $C(N, \\{1, 3\\})$ for $N=14$. The reason might be that $C(N, \\{1, 2\\})$ has a smaller independence number $\\alpha$.\n\nLet's try to implement $C(N, \\{1, 2\\})$ as the default for $N \\geq 4$. This was the \"Inspiration 1\" program. The score for that was 1.1756, which corresponds to $c_{log} = 0.8244$. The current program has a score of 0.5573, corresponding to $c_{log} = 1.4427$. The score is higher for lower $c_{log}$. So 1.1756 is better than 0.5573.\n\nThe current program's $c_{log}$ is 1.4427. The previous best program (which is \"Inspiration 1\") had $c_{log} = 0.8244$. The goal is to *minimize* $c_{log}$. So the previous program was better. The change that led to the current program was switching from $S=\\{1, 2\\}$ to $S=\\{1, 3\\}$, which worsened the $c_{log}$.\n\nTherefore, the improvement needed is to revert to using $S=\\{1, 2\\}$.\n\n```diff\n<<<<<<< SEARCH\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n=======\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```\n\nThis change reverts to the $C(N, \\{1, 2\\})$ construction, which previously yielded a much better $c_{log}$ (0.8244 vs 1.4427). This is the most direct way to improve the current program's performance based on the history.\n\nLet's double check the problem constraints and goals. We want to *minimize* $c_{log}$. The current program has $c_{log} = 1.4427$. The previous best program had $c_{log} = 0.8244$. This means the current program is worse. Reverting to $S=\\{1, 2\\}$ should bring back the better performance.\n\nThe prompt also mentions exploring other directions: \"Structured seeds for general N: circulant C(N, S) with a carefully chosen connection set S; bipartite / tripartite blow-ups; Cayley graphs on small groups; products of small K4-free graphs (Mycielski, Moebius-Kantor, Petersen).\"\nThe current approach is using circulant graphs. The change proposed is to select a better connection set $S$.\n\nThe Paley graph $P(17)$ is a good benchmark. It has $N=17$, $\\alpha=9$, $d_{max}=8$.\n$c_{log}(P(17)) = 9 \\times 8 / (17 \\times \\ln(8)) \\approx 72 / (17 \\times 2.079) \\approx 72 / 35.343 \\approx 2.037$.\nWait, the prompt says $c_{log} \\approx 0.679$ for Paley(17). Let me re-calculate.\n$N=17$, $\\alpha=9$, $d_{max}=8$.\n$c_{log} = \\frac{\\alpha \\cdot d_{max}}{N \\ln(d_{max})} = \\frac{9 \\times 8}{17 \\times \\ln(8)} = \\frac{72}{17 \\times 2.07944} \\approx \\frac{72}{35.3505} \\approx 2.0367$.\nThere might be a misunderstanding of the formula or the Paley graph properties.\nLet's check the definition of $c_{log}$ in the prompt.\n$c_{log} = \\alpha(G) \\cdot d_{max}(G) / (N \\cdot \\ln(d_{max}(G)))$.\nFor Paley graph $P(q)$ where $q$ is a prime power, $q \\equiv 1 \\pmod 4$. Paley graph $P(17)$: $N=17$. $d_{max}=8$. $\\alpha = (q-1)/2 = (17-1)/2 = 8$. Ah, $\\alpha=8$, not 9.\n$c_{log}(P(17)) = \\frac{8 \\times 8}{17 \\times \\ln(8)} = \\frac{64}{17 \\times 2.07944} \\approx \\frac{64}{35.3505} \\approx 1.8104$.\nThis is still not 0.679.\n\nPerhaps the prompt meant $N \\ln(\\alpha)$ or $N \\ln(N)$ in the denominator?\nOr perhaps the $\\alpha$ value for Paley(17) is different?\nThe independence number of Paley graph $P(q)$ is $(q-1)/2$. So for $P(17)$, $\\alpha = (17-1)/2 = 8$.\nThe degree is $q-1 = 16$. Oh, the degree is 16, not 8.\n$N=17$, $d_{max}=16$, $\\alpha=8$.\n$c_{log}(P(17)) = \\frac{8 \\times 16}{17 \\times \\ln(16)} = \\frac{128}{17 \\times 2.77259} \\approx \\frac{128}{47.134} \\approx 2.715$.\n\nThis is very confusing. Let's assume the prompt's stated benchmark value of $c_{log} \\approx 0.679$ for Paley(17) is correct and the formula is also correct. This would imply different $\\alpha$ or $d_{max}$ values are being used by the evaluator for Paley(17).\n\nLet's trust the current program's evaluation metrics. The best score achieved was 1.1756 with $c_{log}=0.8244$ using $C(N, \\{1, 2\\})$. The current program has $c_{log}=1.4427$. Reverting to $C(N, \\{1, 2\\})$ is the most direct way to improve.\n\nThe prompt also says: \"a random K4-free greedy sits around c_log ~= 1.1-1.2\".\nThe baseline greedy in the code has $c_{log} \\approx 1.10-1.17$.\nThe current program's $c_{log}=1.4427$ is worse than random greedy.\nThe previous program's $c_{log}=0.8244$ is better than random greedy.\n\nSo, the change is to revert to $S=\\{1, 2\\}$.\n\nFinal check on the diff:\nThe search part correctly identifies the block where $S$ is defined.\nThe replace part correctly sets $S = \\{1, 2\\}$.\nThe rest of the code within the `else` block remains the same.\nThis change directly addresses the observed performance degradation and aims to restore the better performance seen previously.\n\nOne final thought: could there be a better set $S$ for $C(N, S)$? For example, for $N=14$, what about $S=\\{1, 4\\}$? $d_{max}=4$. $N(0) = \\{1, 13, 4, 10\\}$.\nEdges in $N(0)$:\n- $1$ and $4$? $1-4 = -3 \\equiv 11 \\pmod{14}$. $11 \\not\\in S$.\n- $1$ and $10$? $1-10 = -9 \\equiv 5 \\pmod{14}$. $5 \\not\\in S$.\n- $1$ and $13$? $1-13 = -12 \\equiv 2 \\pmod{14}$. $2 \\not\\in S$.\n- $4$ and $10$? $4-10 = -6 \\equiv 8 \\pmod{14}$. $8 \\not\\in S$.\n- $4$ and $13$? $4-13 = -9 \\equiv 5 \\pmod{14}$. $5 \\not\\in S$.\n- $10$ and $13$? $10-13 = -3 \\equiv 11 \\pmod{14}$. $11 \\not\\in S$.\nSo $C(14, \\{1, 4\\})$ is also K4-free. It's hard to predict without computing $\\alpha$.\n\nGiven the history, reverting to $S=\\{1, 2\\}$ is the most certain improvement.\n\n```python\n# Current code snippet:\n    else:\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n```\n\n```python\n# Proposed code snippet:\n    else:\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n```\nThis is exactly what the diff proposes.\n\n\nOne last consideration: the prompt mentions \"complexity, diversity\". The current program is simple. Reverting to $S=\\{1, 2\\}$ keeps it simple. If we were to explore more complex constructions, we'd need to be very careful about the K4-freeness and the $N$ parameter. For now, optimizing the existing structure is the priority.\n\nThe current program's $c_{log}$ is 1.4427.\nThe previous best program's $c_{log}$ was 0.8244.\nThe goal is to *minimize* $c_{log}$.\nTherefore, reverting to the previous program's construction of $C(N, \\{1, 2\\})$ is the correct step.\n\nThe difference in scores (0.5573 vs 1.1756) also indicates the change from 1.1756 to 0.5573 was detrimental. Since higher scores are better, 1.1756 is superior to 0.5573.\n\nThus, the proposed change is to restore the $C(N, \\{1, 2\\})$ construction.\n\n```diff\n<<<<<<< SEARCH\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n=======\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```\n\nThis change directly addresses the performance drop and aims to restore the better performing $C(N, \\{1, 2\\})$ construction.\n\n```python\n# Reasoning for the change:\n# The current program uses a circulant graph C(N, {1, 3}) for N >= 4.\n# The history shows that a previous version using C(N, {1, 2}) achieved a\n# significantly better c_log score (0.8244 vs 1.4427).\n# The goal is to minimize c_log. Therefore, reverting to the C(N, {1, 2})\n# construction is expected to improve the program's fitness.\n# Both C(N, {1, 2}) and C(N, {1, 3}) are K4-free for sufficiently large N\n# and are regular of degree 4. The difference in their performance\n# likely stems from their respective independence numbers (alpha).\n# The change restores the better performing construction.\n``````diff\n<<<<<<< SEARCH\n        # Construct C(N, {1, 3}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 3} # Changed connection set from {1, 2} to {1, 3}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n=======\n        # Construct C(N, {1, 2}) for N >= 4.\n        # This graph is K4-free, regular of degree 4.\n        adj = np.zeros((N, N), dtype=np.uint8)\n        S = {1, 2}\n        for i in range(N):\n            for s in S:\n                neighbor = (i + s) % N\n                adj[i, neighbor] = 1\n                adj[neighbor, i] = 1\n        return adj\n# EVOLVE-BLOCK-END\n>>>>>>> REPLACE\n```\n# Reasoning for the change:\n# The current program uses a circulant graph C(N, {1, 3}) for N >= 4.\n# The execution history indicates that a previous version using C(N, {1, 2})\n# achieved a significantly better c_log score (0.8244) compared to the\n# current program's c_log (1.4427). The objective is to minimize c_log.\n# Therefore, reverting to the C(N, {1, 2}) construction is expected to\n# improve the program's fitness score. Both C(N, {1, 2}) and C(N, {1, 3})\n# are K4-free and regular of degree 4 for sufficiently large N. The observed\n# difference in performance suggests that C(N, {1, 2}) might have a smaller\n# independence number (alpha), leading to a lower c_log. This change restores\n# the previously better-performing construction."]}}, "artifacts_json": "{\"c_log\": \"0.824397\", \"alpha\": \"4\", \"d_max\": \"4\", \"edges\": \"28\", \"trials\": \"trial 0: c_log=0.8244 score=1.1756 (0.00s)\\ntrial 1: c_log=0.8244 score=1.1756 (0.00s)\\ntrial 2: c_log=0.8244 score=1.1756 (0.00s)\"}", "artifact_dir": null, "embedding": null}