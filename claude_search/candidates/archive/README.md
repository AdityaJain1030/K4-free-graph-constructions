# Archived candidates (claude_search session, 2026-04-22)

97 Python files generated by the Claude Code optimizer agent during a single
~3-hour session. Each file implements a `construct(N) -> list[tuple[int,int]]`
function targeting non-VT K₄-free graphs.

**Outcome:** best c = 0.9593, target was < 0.6789. None beat P(17).

Files are preserved for reference; the evaluation record is in `../results.jsonl`.
See `../../docs/NON_VT_SEARCH.md` for the full post-mortem.

Family coverage:
- core_periphery (58 attempts) — STALE at c=1.3049
- random_process (9 attempts)
- polarity (12 attempts)
- asymmetric_lift (9 attempts)
- structure_plus_noise, voltage_partial, srg_perturbed, invented, crossover,
  two_orbit (1–3 attempts each)
- perturbed_paley, sat_seeded — never attempted
